public static int findSingleElement(List<Integer> arr) {
         int left = 0;
        int right = arr.size() - 1;

        while (left < right) {

            int mid = left + (right - left) / 2;

            if (mid % 2 == 1) {
                mid--;  
            }

            if (arr.get(mid).equals(arr.get(mid + 1))) {
                left = mid + 2;
            } else {
                right = mid;
            }
        }

        return arr.get(left);
    }